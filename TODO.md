# DynamicForm Reset Bug Debug\n\n## Current State: Storage clears, manual fields reset, storage-loaded \"name\" persists post-submit.\n\n1. [ ] Add these console.logs to onSubmit (temp):\n```\nconst onSubmit = (data: any) => {\n  console.log('Pre-clear storage:', localStorage.getItem(formStorageKey));\n  localStorage.removeItem(formStorageKey);\n  console.log('Post-clear storage:', localStorage.getItem(formStorageKey));\n  reset({});\n  console.log('Post-reset values:', watch());  // Should be {}\n  setFormKey(prev => {\n    console.log('New formKey:', prev + 1);\n    return prev + 1;\n  });\n};\n```\n\n2. [ ] Add to useForm init:\n```\nconsole.log('Mount - storage:', localStorage.getItem(formStorageKey), 'defaults:', defaultValues);\n```\n\n3. [ ] Repro exact steps:\n   - Load contact form.\n   - Fill \"name\" → refresh → persists.\n   - Fill \"email\"/\"subject\" → submit.\n   - Observe: name filled, others empty? Check console/storage tab.\n\n4. [ ] Check src/main.tsx for <React.StrictMode> - disable temporarily.\n\n5. [ ] Test single-tab, incognito. Run `console.clear()` before submit.\n\n6. [ ] After submit, manually refresh - does name persist? (Tests if storage truly cleared).\n\n## Expected Logs for Fix:\n- Post-clear: null\n- Post-reset: {}\n- New mount: null defaults\n\nProgress: 0/6\n
